
<!DOCTYPE html>
<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <meta charset="utf-8">
    <title>To Do List</title>
</head>
<body>
    <div class="container">
        <h1>Welcome to To Do List</h1>
        <div class="row">
            <div class="col-md-8">
                <p>Table goes here</p>
                <div class="row">
                    <div class="col-sm-6">
                        <p>Urgent/Important</p>
                    </div>
                    <div class="col-sm-6">
                        <p>Not Urgent/Important</p>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-6">
                        <p>Urgent/Not Important</p>
                    </div>
                    <div class="col-sm-6">
                        <p>Not Urgent/Not Important</p>
                    </div>
                </div>
            </div>
            <div class="col-sm-4">

                <p>Create a new category:</p>

                <form action="/categories" method="post">
                    <label for="description">Enter a description:</label>
                    <input id="description" name="description" type="text">

                    <label for="importance">How important is this?</label>
                    <select id="importance" name="importance" type="text">
                        <option value="1">Important</option>
                        <option value="2">Not Too Important</option>
                    </select>

                    <label for="due-date">Due-date for task:</label>
                    <input id="due-date" name="due-date" type="date">

                    <label for="category">Category for task:</label>
                    <input id="category" name="category" type="text">

                    <button type="submit">Add category</button>
                </form>
            </div>
        </div>
    </div>

            <!-- {% if categories is not empty %}
            <p>Here are all your categories:</p>
            <ul>
                {% for category in categories %}
                <li><a href="/categories/{{ category.getId }}">{{ category.getName }}</a></li>
                {% endfor %}
            </ul>
            {% else %}
            <p>There are no categories yet!</p>
            {% endif %} -->
    </body>
    </html>
